\documentclass[defaultpackages]{cheatsheet}

\usepackage{lipsum}

\usepackage{graphicx}

\title{An example cheatsheet}
\author{Teodor Dahl Knutsen}

\begin{document}

\tableofcontents

\section{Det første som skjer}

\lipsum[1-1]

\begin{equation}
	\sum_{n=1}^\infty 10^{-n!}=0.110001\cdots\label{eq_1}
\end{equation}

As you can see in Equation~\ref{eq_1}, it is a transcendental number, $3 + 5 = 8$.

\lipsum[2-2]

\[ \int_0^\infty f(x)\, dx = e^x \]

\lipsum[1-1]

\subsection{Poisson Process}

\subsubsection{Relation(probability, intenstiy and mean value function)}
Given a probability for an event of type 'x':\\
The intensity function is the rate $\lambda \cdot probability(x)$\\
The mean value function is the integral of the intensity function:\\
$\int_0^t(\lambda(x))$\\

\subsection{Exponentional variables}
If a variable $T_i$ $\sim$ $exp(t, \lambda$), the sum of Exponentional variables $\sum_{k=1}^{\infty}T_i ~gamma(\alpha, \beta)$ is gamma distributed with mean $k\theta$ or $\frac{\alpha}{\beta}$.\\

\section*{Markov chain}
\subsubsection{Limiting probability}
Enough that the chain is irreducible and\\
The chain is \underline{positive} recurrent.\\
$\pi_i$ is not limiting if the markov chain is not aperiodic(period=1).\\
$\pi_i$ is the probability and also the long run proportions in the discrete chain.\\

\subsubsection{Lorem ipsum dolor sit amet}

\lipsum[1-1]

\section{kake}

\lipsum[3-50]

\end{document}
